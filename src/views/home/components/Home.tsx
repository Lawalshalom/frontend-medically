import React, { FC } from 'react';
import { Button } from 'antd';

const Home: FC = () => {
  return (
    <div>
      This is the home page
      <Button color="primary">My Awesome Button</Button>
    </div>
  );
};

export default Home;
